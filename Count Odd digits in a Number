#program that returns no.of odd digits in a number
def count_odd(n):
  count=0
  while n>0 :
    digit=n%10
    if digit%2!=0:
      count+=1
    n=n//10
return count
n=int(input())
result = count_odd(n)
print(result)

